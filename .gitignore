# ── .NET Build Artifacts ──
bin/
obj/
*.user
*.suo
*.userosscache
*.sln.docstates
*.userprefs

# ── Visual Studio ──
.vs/
*.rsuser
[Dd]ebug/
[Rr]elease/
x64/
x86/
[Ww][Ii][Nn]32/
[Aa][Rr][Mm]/
[Aa][Rr][Mm]64/

# ── NuGet ──
*.nupkg
**/[Pp]ackages/*
!**/[Pp]ackages/build/
*.nuget.props
*.nuget.targets
project.assets.json
NuGet.Config

# ── Compiler Output ──
*.ilk
*.pdb
*.exp
*.lib
*.sbr
*.bsc
*.iobj
*.ipdb
*.[Cc]ache
*.appxsym
*.eappxsym

# ── Shader Compilation (keep .hlsl source, ignore compiled) ──
*.cso
*.dxbc

# ── Publish Output (keep PACKAGE.md + PLAN) ──
publish/*
!publish/PACKAGE.md
!publish/PLAN/
!publish/PLAN/**

# ── Publish 内的测试数据 (raw 像素数据, 体积大且无上传价值) ──
# 注意: 必须放在 !publish/PLAN/** 白名单规则之后才能生效
publish/PLAN/tools/*.raw

# ── 重复模型目录 (2026-08-25 清理: 与 PLAN/models 完全相同且目录名误导,
#    实际为 FP32 文件; Publish.ps1 仅使用 PLAN/models/) ──
publish/PLAN/models_fp16/
publish/PLAN/models_fp32/

# ── 手动测试/调试脚本 (统一管理, 不上传 GitHub) ──
tests/manual/

# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# ── .NET 11 Migration Backup ──
.net10-backup/

# ── OS junk ──
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Desktop.ini

# ── Crash / Log files ──
*.log
crash.log
*_crash.log

# ── 日志目录 (运行时产生, 禁止上传) ──
log/
**/log/
!publish/PACKAGE.md

# ── TTC 日志查看器 (独立可选工具, 发布产物不入库) ──
publish/TTC日志查看器/

# ── 性能基准/调试产物 (本地调试用, 不入库) ──
ocr_performance_*.md
perf_result*.txt
run_ocr_perf_benchmark.ps1
scripts/Check*.ps1

# ── Temp / Accidental files ──
*_error.log
*.dmp

# ── IDE / Editor ──
*.swp
*.swo
*~
.idea/
*.sublime-*
.vscode/
*.code-workspace

# ── Temp / Test outputs ──
TestResults/
*.tmp
*.temp

# ── 冒烟测试截图 (运行时产生, 禁止上传) ──
scripts/*.png

# ── TrueToneCap specific ──
TrueToneCap.settings.json

# ── 签名密钥 (防泄露) ──
*.pfx
*.snk
*.spc
*.cer

# ── 测试输出图片/临时产物 ──
*.test.png
*.test.jpg
